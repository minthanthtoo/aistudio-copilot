{
  "name": "aistudio-copilot",
  "version": "0.3.0",
  "private": true,
  "description": "A prompt engineering assistant and queue automation copilot for Google AI Studio.",
  "scripts": {
    "icons": "node scripts/generate-icons.cjs",
    "check": "node --check src/core.js && node --check src/spec-engine.js && node --check src/chatgpt-extractor.js && node --check src/content.js && node --check src/background.js && node --check scripts/generate-icons.cjs && node --check scripts/package-extension.cjs",
    "test": "node --test tests/*.test.cjs",
    "verify": "npm run check && npm test",
    "package:extension": "npm run verify && node scripts/package-extension.cjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
