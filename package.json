{
  "name": "aistudio-copilot",
  "version": "0.3.7",
  "private": true,
  "description": "A prompt engineering assistant and queue automation copilot for Google AI Studio.",
  "scripts": {
    "icons": "node scripts/generate-icons.cjs",
    "check": "for file in src/*.js; do node --check \"$file\" || exit 1; done && for file in scripts/*.cjs; do node --check \"$file\" || exit 1; done",
    "test": "node --test tests/*.test.cjs",
    "verify": "npm run check && npm test",
    "package:extension": "npm run verify && node scripts/package-extension.cjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
